<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
	"-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
	"http://struts.apache.org/dtds/struts-2.3.dtd">

<struts>
    <constant name="struts.enable.DynamicMethodInvocation" value="false" />
    <constant name="struts.devMode" value="false" />
       <!--
          1. package name : 요청 uri 개념. 호출시 별도로 지정하지 않으면 default가 사용된다.
          2. action name, class : 요청이 처리될 action 파일 지정
          3. result name : action 파일의 return string과 매칭되는 파일을 view로 사용함
        --> 

	<!-- Package Name 은 여기서만 사용.. namespace 는 url 경로지정-->
	
    <include file="travelmap.xml"></include>
</struts>

 
